// AUTO-GENERATED by protoc-gen-webviewrpc
// JavaScript Server: OpenURLServiceServiceBase

// 메서드별 인코딩 함수를 가져옴
// Get encoding/decoding functions for each method
import { decodeOpenURLRequest, encodeOpenURLResponse, } from './OpenURLService.js';

/**
 * 추상 클래스 (C#의 OpenURLServiceBase)
 * 사용자(서버구현자)는 이 클래스를 상속해서 실제 로직을 override한다.
 * Abstract class (like C#'s OpenURLServiceBase)
 * Users (server implementors) should inherit this class and override the methods.
 */
export class OpenURLServiceBase {
  
  /**
   * async OpenURL
   * @param { OpenURLRequest } requestObj
   * @returns {Promise< OpenURLResponse >}
   */
  async OpenURL(requestObj) {
    throw new Error("Method OpenURL must be implemented");
  }
  
}

/**
 * static BindService, (C#의 OpenURLService.BindService(impl))
 * - impl: OpenURLServiceBase implementation
 * - return: ServiceDefinition(methodHandlers)
 */
export class OpenURLService {
  static bindService(impl) {
    const def = {
      methodHandlers: {}
    };

    
    def.methodHandlers["OpenURLService.OpenURL"] = async (reqBytes) => {
      const reqObj = decodeOpenURLRequest(reqBytes);
      const respObj = await impl.OpenURL(reqObj);
      return encodeOpenURLResponse(respObj);
    };
    

    return def;
  }
}
